#!/usr/bin/env node

'use strict'

console.timeEnd('Launch')

module.exports = (async () => {
    'use strict'

    const config = require('./import/config')
    const prisma = require('./import/mix')

    return false

})(0)
